# verse-lingua completions

complete -c verse-lingua -l input-folder-path -r -d "Input folder path (native chapters)"
complete -c verse-lingua -l output-folder-path -r -d "Output folder path (translated chapters)"

complete -c verse-lingua -l help -d "Show help and exit"